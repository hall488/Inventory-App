extends layout 

block content 
    h1 #{title}

    form(method='POST' action='')
        label(for='name')
        input(name='name' placeholder='Enter Name')
        button(type='submit') Create Deck
    