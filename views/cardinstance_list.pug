extends layout

block content
  h1= title

  ul
    each val in cardinstance_list
      li
        a(href=val.url) #{val.card.name}
        if val.deck != null
            div 
                |#{val.deck}

    else
      li There are no card instances in this library.
